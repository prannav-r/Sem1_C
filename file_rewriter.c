#include <stdio.h>

int main(){
    FILE *fp,*fp2;
    fp=fopen("hi.txt","r");
    fp2=fopen("bye.txt","w");
    int x,n=-1;
    char ch[1000],new[1000];
    printf("Enter the line you want to replace:");
    scanf("%d",x);
    printf("Enter the new line :");
    gets(new);
    while(n!=x){
         fgets(ch,)
    }
}